---
description: Split the current tmux pane and open bash in the current worktree path (-h horizontal default, -v vertical)
allowed-tools: Bash
model: claude-haiku-4-5-20251001
---

```bash
bash "${HOME}/.claude/scripts/open-bash.sh" "$ARGUMENTS"
```

Print the output verbatim.
