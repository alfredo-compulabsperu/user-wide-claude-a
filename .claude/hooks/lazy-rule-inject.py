#!/usr/bin/env python3
"""Task 2.1 stub: exits 0 emitting nothing, so every test in
tests/test_lazy_rule_inject.py fails on its own assertion rather than on a
missing file. Replaced by the real injector in Task 2.2."""
import sys

sys.stdin.read()
sys.exit(0)
